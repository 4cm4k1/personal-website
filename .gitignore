# directories ---------------
# `next build` outputs
.next
# `vercel` account config
.vercel
# `webpack-bundle-analyzer` outputs
bundles
# `yarn` packages
node_modules
# files ---------------------
# logs
*.log
