targets:
  $default:
    builders:
      build_web_compilers|entrypoint:
        generate_for:
        - web/**.dart
        options:
          compiler: dart2js
          dart2js_args:
          - --csp
          - --dump-info
          - --enable-diagnostic-colors
          - --fast-startup
          - --trust-primitives
          - --trust-type-annotations
        release_options:
          dart2js_args:
          - --csp
          - --dump-info
          - --fast-startup
          - --minify
          - --trust-primitives
          - --trust-type-annotations
